package com.caylakym.aberritual.data.model

enum class TiltAxis(val id: String, val displayName: String) {
    HORIZONTAL("horizontal", "Horizontal (Left / Right)"),
    VERTICAL("vertical", "Vertical (Up / Down)")
}
